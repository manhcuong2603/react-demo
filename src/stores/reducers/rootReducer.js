const initState = {
    users:[
        {id:1, name:'cuong'},
        {id:2, name:'sinh vien'}
    ]
}
const rootReducer = (state = initState, action) => {

    return state;
}
export default rootReducer;